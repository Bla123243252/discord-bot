Config = {}

-- ZENITH-Banner (liegt in dist/banners/, wird über den NUI-Asset-Endpoint geladen).
-- Gilt als Standard für ALLE Menüs ohne eigenen header-Parameter.
Config.DefaultHeader = 'https://cfx-nui-hex_menu_api/dist/banners/zenith.png'

Config.RightClickMenuClose = true -- Set to false if you don't want the menu to close when pressing the right mouse button